Logo image used by the app:
  thelittleurl-logo.png

This file is used as:
- The left icon in the "The Little URL" logo (landing, dashboard, auth, admin).
- Favicon and apple-touch icon (browser tab and bookmarks).

Recommended: Square image, at least 512x512 px for crisp display at all sizes.
The app scales it per context (nav, sidebar, auth pages, favicon).
